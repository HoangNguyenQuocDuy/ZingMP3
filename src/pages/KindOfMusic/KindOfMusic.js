import classNames from "classnames/bind";

import styles from './kindOfMusic.module.scss';

const cx = classNames.bind(styles);

function KindOfMusic() {
    return ( 
        <h1>KindOfMusic</h1>
     );
}

export default KindOfMusic;